# Web framework für Backend-Entwicklung (LTS-Version für Stabilität)
Django>=4.2,<=5.1.2

# Framework für RESTful APIs in Django
djangorestframework==3.16.1

# JSON Web Token (JWT) Authentifizierung für Django REST Framework
djangorestframework-simplejwt==5.3.1

# CORS-Header für Cross-Origin-Requests
django-cors-headers==4.7.0

# Redis-Cache-Backend für Django
django-redis==5.4.0

# Asynchrone Aufgabenverarbeitung mit Redis
django-rq==2.10.3  # Korrigiert von 2.10.0 auf 2.10.3 (verfügbar auf PyPI)

# WSGI-Server für Django in Produktion
gunicorn==22.0.0

# Bildverarbeitungsbibliothek
Pillow==10.4.0

# PostgreSQL-Adapter für Django
psycopg[binary]==3.2.3

# Lädt Umgebungsvariablen aus .env-Dateien
python-dotenv==1.0.1

# Statische Dateien in Django ohne separaten Webserver
whitenoise==6.7.0

# Test-Framework für Python
pytest==8.3.3

# Pytest-Plugin für Django-spezifische Tests
pytest-django==4.9.0

# Pytest-Plugin für Testabdeckungsmessung
pytest-cov==5.0.0

# Misst Testabdeckung für Python-Code
coverage==7.6.3